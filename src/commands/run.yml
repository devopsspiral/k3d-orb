description: Wrapper around run command with helpers included
parameters:
  command:
    description: Command using docker, k3d, helm, kubectl
    type: string
    default: |
      k3d --version
      helm version
      kubectl version
steps:
  - run:
      name: Run k3d command
      command: |
        . helpers.sh
        <<parameters.command>>
